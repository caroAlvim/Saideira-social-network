import {
  loginPage,
  signInGoogleAccount,
  createUser,
  currentUser,
  logout,
  updateUserName,
  updateUserImage,
  uploadImage,
  uploadImageBooks,
  forgotPassword,
  createReview,
  getReviews,
  getPost,
  like,
  sendComment,
  deleteComment,
  deletePost,
  editReview,
  saveReview,
  save,
  getSavedReviews,
  getProfileReviews,
} from './index.js';

describe('loginPage', () => {
  it('should be a function', () => {
    expect(typeof loginPage).toBe('function');
  });
});

describe('signInGoogleAccount', () => {
  it('should be a function', () => {
    expect(typeof signInGoogleAccount).toBe('function');
  });
});

describe('createUser', () => {
  it('should be a function', () => {
    expect(typeof createUser).toBe('function');
  });
});

describe('currentUser', () => {
  it('should be a function', () => {
    expect(typeof currentUser).toBe('function');
  });
});

describe('logout', () => {
  it('should be a function', () => {
    expect(typeof logout).toBe('function');
  });
});

describe('updateUserName', () => {
  it('should be a function', () => {
    expect(typeof updateUserName).toBe('function');
  });
});

describe('updateUserImage', () => {
  it('should be a function', () => {
    expect(typeof updateUserImage).toBe('function');
  });
});

describe('uploadImage', () => {
  it('should be a function', () => {
    expect(typeof uploadImage).toBe('function');
  });
});

describe('uploadImageBooks', () => {
  it('should be a function', () => {
    expect(typeof uploadImageBooks).toBe('function');
  });
});

describe('forgotPassword', () => {
  it('should be a function', () => {
    expect(typeof forgotPassword).toBe('function');
  });
});

describe('createReview', () => {
  it('should be a function', () => {
    expect(typeof createReview).toBe('function');
  });
});

describe('getReviews', () => {
  it('should be a function', () => {
    expect(typeof getReviews).toBe('function');
  });
});

describe('getPost', () => {
  it('should be a function', () => {
    expect(typeof getPost).toBe('function');
  });
});

describe('like', () => {
  it('should be a function', () => {
    expect(typeof like).toBe('function');
  });
});

describe('sendComment', () => {
  it('should be a function', () => {
    expect(typeof sendComment).toBe('function');
  });
});

describe('deleteComment', () => {
  it('should be a function', () => {
    expect(typeof deleteComment).toBe('function');
  });
});

describe('deletePost', () => {
  it('should be a function', () => {
    expect(typeof deletePost).toBe('function');
  });
});

describe('editReview', () => {
  it('should be a function', () => {
    expect(typeof editReview).toBe('function');
  });
});

describe('saveReview', () => {
  it('should be a function', () => {
    expect(typeof saveReview).toBe('function');
  });
});

describe('save', () => {
  it('should be a function', () => {
    expect(typeof save).toBe('function');
  });
});

describe('getSavedReviews', () => {
  it('should be a function', () => {
    expect(typeof getSavedReviews).toBe('function');
  });
});

describe('getProfileReviews', () => {
  it('should be a function', () => {
    expect(typeof getProfileReviews).toBe('function');
  });
});
